from user_interface.class_user_interface import User_Interface

# создаётся и вызывается объект взаимодействия с пользователем,
# содержащий основные команды
if __name__ == '__main__':

    func = User_Interface()
    func()
